#ifndef __SDLK_TYPES__
#define __SDLK_TYPES__

    #include <iostream>

    namespace Sdlk{

        class Positions{
            public:
                int _x, _y;
                Positions(int x ,int y);
        };
    }
#endif